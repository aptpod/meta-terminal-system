# Suppress relinquish of /var/log/journal at shutdown: the default
# ExecStop=journalctl --smart-relinquish-var switches journald's write
# target to /run/log/journal (tmpfs) which is wiped on reboot.
[Service]
ExecStop=
